#include <stdio.h>
//Consider the following C program
int main(void)
{
    int i, j, k = 0;
    j = 2 * 3 / 4 + 2.0 / 5 + 8 / 5;
    f -= --j;

    for (int i = 0; i < 5; i++)
    {
        switch (i + k)
        {
        case 1:
            /* code */
        case 2:
            printf("\n%d", i + k);
        case 3:
            printf("\n%d", i + k);

        default:
            printf("\n%d", i + k);
        }
    }

    return 0;
}
/*
The numebr of times printf statement is executed is ____
a). 8 times
b). 9 times
c). 10 times => correct
d). 11 times
*/

// Code By: Avinash Kumar.