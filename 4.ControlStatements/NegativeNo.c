#include <stdio.h>
//Program to check input number is negative or not
int main(void)
{
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);

    if (numm < 0)
    { //Block of code
        printf("Number entered is negative\n");
        num = -num;
    }

    printf("Value of num is: %d\n", num);

    return 0;
}
// Code By: Avinash Kumar.